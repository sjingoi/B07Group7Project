<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />
    <stroke
        android:width="2dp"
        android:color="@color/border_color" /> <!-- Set your desired border color here -->
    <corners android:radius="8dp" /> <!-- Match this with the cardCornerRadius value -->
</shape>